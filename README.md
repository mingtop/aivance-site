# AIVANCE Website (Static, GitHub Pages Ready)

This repository contains the static landing page for **AIVANCE**.

## 📦 Structure
- `index.html` — single‑file site (vanilla HTML/CSS/JS, responsive).
- `assets/logo.svg` — vector brand mark.
- `assets/social-card.png` — placeholder Open Graph image.
- `LICENSE` — MIT license (optional for public repos).

## 🚀 Deploy on GitHub Pages
1. Create a new repo on GitHub, e.g. `aivance-site`.
2. Upload all files in this folder (or push via git).
3. On GitHub, open **Settings → Pages**:
   - **Build and deployment**: `Deploy from a branch`
   - **Branch**: `main` (or `master`) / root `/`
4. Your site will appear at `https://<your-username>.github.io/aivance-site/`.
   - (Optional) Add a custom domain in **Pages → Custom domain** and create a CNAME DNS record.

## ✏️ Edit
- Update contact email in `index.html` (`hello@aivance.ai`).
- Replace `assets/social-card.png` with a proper banner (1200×630).

## 🛟 Contact
hello@aivance.ai
